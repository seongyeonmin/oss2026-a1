# A1 리포트

- 이름: 민성연
- 학번: 2025402004 
- GitHub ID: seongyeonmin

## 어디를 둘러봤는지

열어본 awesome-nodejs 카테고리, 터미널에서 써본 검색어, 링크를 따라간 경로 등을 적습니다.
 
 -> awesome-nodejs 저장소에서 `Mad science`와 `Weird` 카테고리를 먼저 둘러보았습니다. 그 후 터미널에서 `npm search "cli table"`을 실행해보고 npmjs.com에서 키워드 파도타기를 시작했습니다. 평소 관심 있던 분야라 `NodeOS`를 검색해 보았고, 결과로 나온 `nodeos-cross-toolchain` 패키지에서 `embedded`, `linux` 키워드를 따라가며 관련 패키지들을 집중적으로 구경했습니다.


---

## 선정한 패키지

### 1. `<nodeos-cross-toolchain>`

**선정 이유:** 
-> 임베디드 환경과 관련된 Node 패키지가 있다는 점이 흥미로워서 선정했습니다.


**이것으로 무엇을 할 수 있을지:**
-> 노드 기반의 임베디드 시스템 운영체제를 구축하거나, 크로스 컴파일을 위한 툴체인을 구성하는데 활용해 볼 수 있을 것 같습니다.


**확인 결과:**

```
$ npm view <패키지이름> version time.modified license dependencies

version = '1.0.0-RC3.1'
time.modified = '2022-06-22T03:16:35.060Z'
license = 'MIT'
dependencies = { builddependencies: '^0.0.1', 'prebuild-install': '2.1.0' }


$ npm view <패키지이름> deprecated

출력없음
```

**출력을 보고 알게 된 것:**
-> 라이선스가 MIT로 설정되어 있어 누구나 비교적 자유롭게 가져다 쓰고 수정할 수 있는 오픈소스라는 것을 알 수 있었습니다.

---

### 2. `<npkill>`

**선정 이유:**
-> 설명을 보니 컴퓨터 용량을 많이 차지하는 node_modules 폴더를 쉽게 찾아서 지워준다는 점이 실용적이고 유용해보여서 선정했습니다.


**이것으로 무엇을 할 수 있을지:**
-> 예전에 연습용으로 만들고 방치해둔 수많은 코딩 프로젝트 폴더들을 한 번에 스캔해서, 불필요한 용량을 차지하는 파일들을 싹 정리해 컴퓨터 저장 공간을 쾌적하게 확보하는 데 쓸 수 있을 것 같습니다.


**확인 결과:**
version = '0.12.2'
time.modified = '2024-11-25T09:34:08.526Z'
license = 'MIT'
dependencies = {
  rxjs: '^7.8.1',
  colors: '1.4.0',
  'node-emoji': '^2.1.3',
  'ansi-escapes': '^6.2.1',
  'get-folder-size': '^4.0.0',
  'open-file-explorer': '^1.0.2'
}
```
출력없음(정상)
```

**출력을 보고 알게 된 것:**
마지막 업데이트 날짜가 2024년도 11월로 생각보다 업데이트가 최근에 된 패키지라는 것을 알게 되었습니다.

---

### 3. `<lodash>`

**선정 이유:**
-> 주간 다운로드 수가 1억 5천만 건이 넘는 것을 보고, 도대체 얼마나 유용하고 필수적인 패키지이길래 이렇게 많이 쓰이는지 궁금해서 선정했습니다.


**이것으로 무엇을 할 수 있을지:**
->자바스크립트로 배열이나 객체 같은 복잡한 데이터를 다룰 때 코드를 훨씬 깔끔하고 짧게 만들어주는 도구라고 알고 있습니다. 나중에 프로젝트에서 많은 양의 사용자 데이터나 센서값 등을 정렬하고 필터링해야 할 때 유용하게 활용해 볼 수 있을 것 같습니다.


**확인 결과:**
version = '4.18.1'
time.modified = '2026-09-15T03:50:20.592Z'
license = 'MIT'
```

```

**출력을 보고 알게 된 것:**
패키지에 의존하지 않는다는 것을 확인했다.

---

## 설치해본 패키지

```
$ npm install lodash

$ node try.js

```
출력

전체 언어: [ 'javascript', 'python', 'java', 'c++' ]
두 개씩 묶기: [ [ 'javascript', 'python' ], [ 'java', 'c++' ] ]
---

## 막혔던 부분 (채점하지 않음)

에러 메시지든 헷갈렸던 부분이든 하나. 두 문장이면 됩니다. 없었으면 없었다고 적습니다.

```

```

---

## AI 사용

사용했다면 프롬프트와, AI의 설명이 실제와 달랐던 부분을 적습니다.
사용하지 않았다면 "사용하지 않음"이라고만 적으면 됩니다.

---

## 제출 전 확인

- [ ] 저장소 이름이 `oss2026-a1`, 공개 범위가 Public
- [ ] `git status` 결과가 `nothing to commit, working tree clean`
- [ ] `node_modules` 폴더를 지우고 `npm install` → `node try.js` 를 다시 해도 실행됨
- [ ] 마지막 커밋을 push함
